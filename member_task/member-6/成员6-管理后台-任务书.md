# 成员6 任务书 — 管理后台工程师（全栈，精简版）

> **角色**：平台运营管控者（MVP精简版）  
> **权重**：⭐⭐（较轻，MVP阶段管理后台功能最少）  
> **工作量最小，但你的管理后台是独立工程，需要从零搭建**

---

## 你的核心任务

```
              ┌─────────────────────────────┐
              │       成员6 负责范围           │
              └──────────────┬──────────────┘
                             │
        ┌────────────────────┴────────────────────┐
        ▼                                         ▼
┌──────────────────┐                    ┌──────────────────┐
│   管理后端API     │                    │   管理前端页面     │
│   3个接口        │                    │   2-3个页面      │
│   商品审核       │                    │   审核页         │
│   基础统计       │                    │   统计页         │
└──────────────────┘                    │   登录页         │
                                        └──────────────────┘
```

---

## 任务清单（按天拆解）

### Day 1（7月7日）：理解项目 + 创建Entity/Mapper

| 序号 | 具体任务 | 产出文件 |
|------|---------|---------|
| 1.1 | 精读PRD中管理后台需求 | — |
| 1.2 | 创建 `Blacklist` 实体类 | `entity/Blacklist.java` |
| 1.3 | 创建 `AuditLog` 实体类 | `entity/AuditLog.java` |
| 1.4 | 创建 `BlacklistMapper` | `mapper/BlacklistMapper.java` |
| 1.5 | 创建 `AuditLogMapper` | `mapper/AuditLogMapper.java` |

> 注意：管理员账号在初始化SQL中手动插入，MVP不需要管理员注册功能。

---

### Day 2（7月8日）：商品审核API

| 序号 | 具体任务 | API路径 |
|------|---------|---------|
| 2.1 | 创建 `AdminService` 接口 + `AdminServiceImpl` | — |
| 2.2 | 实现 `GET /api/admin/product/pending` — 待审核商品列表（分页，JOIN商品表和用户表显示发布者信息） | `GET /api/admin/product/pending?page=1&size=20` |
| 2.3 | 实现 `POST /api/admin/product/audit` — 审核商品（通过：status改为已上架 / 驳回：status改为审核失败+记录原因） | `POST /api/admin/product/audit` |
| 2.4 | 实现 `GET /api/admin/dashboard` — 数据看板（仅基础统计数字，不做图表） | `GET /api/admin/dashboard` |
| 2.5 | 创建 `AdminController`，所有接口加 `@AdminOnly` 注解 | — |

**审核请求体**：
```json
{
  "productId": 1,
  "action": "APPROVE",    // APPROVE or REJECT
  "reason": "商品图片与描述不符"  // 驳回时必填
}
```

**数据看板返回示例**（简化版，只返回数字）：
```json
{
  "totalUsers": 128,
  "totalProducts": 356,
  "totalOrders": 89,
  "todayNewUsers": 5,
  "todayNewProducts": 12,
  "todayOrders": 3
}
```

---

### Day 3（7月9日）：管理端前端工程搭建

| 序号 | 具体任务 | 说明 |
|------|---------|------|
| 3.1 | 用Vite创建独立的管理端React项目 `admin-app/` | 独立工程，不与用户端共用 |
| 3.2 | 安装依赖：antd, react-router-dom, axios | — |
| 3.3 | 配置Ant Design Pro风格布局：左侧菜单 + 顶部导航 | `App.tsx` |
| 3.4 | 配置路由：`/admin/login`（登录）, `/admin/dashboard`（统计）, `/admin/audit`（审核） | `router/index.tsx` |
| 3.5 | 封装Axios实例（baseURL指向后端，请求拦截器带JWT token） | `api/request.ts` |

**管理端路由**：
```
/admin/login       → 管理员登录页
/admin/dashboard   → 数据看板（统计数字+简单列表）
/admin/audit       → 商品审核页
```

---

### Day 4（7月10日）：管理端前端页面（1）

| 序号 | 具体任务 | 路由 |
|------|---------|------|
| 4.1 | 管理员登录页：手机号+密码登录 | `/admin/login` |
| 4.2 | 商品审核页：待审核商品列表（Ant Design Table，显示商品标题/图片/分类/价格/发布者/发布时间） | `/admin/audit` |
| 4.3 | 商品审核页：审核操作（行内按钮：通过/驳回，驳回时弹出Modal输入原因） | `/admin/audit` |

---

### Day 5（7月11日）：管理端前端页面（2）

| 序号 | 具体任务 | 路由 |
|------|---------|------|
| 5.1 | 数据看板页：4个统计卡片（Ant Design Statistic组件：总用户数/总商品数/总订单数/今日新增） | `/admin/dashboard` |
| 5.2 | 数据看板页：今日数据明细（今日新增用户/商品/订单的简单列表） | `/admin/dashboard` |

> 不做ECharts图表！只展示数字+简单列表。

---

### Day 6~7（7月14~15日）：收尾 + 自测

| 任务 | 说明 |
|------|------|
| 管理端全流程自测：管理员登录→查看待审核商品→审核通过→查看统计 | — |
| 如果时间充裕，可额外实现：黑名单管理（加入/移除黑名单用户） | 加分项 |
| 如果时间充裕，可额外实现：类目管理（新增/编辑/删除商品类目） | 加分项 |

---

### Day 8~10（7月16~18日）：联调 + Bug修复 + 支援

| 任务 | 说明 |
|------|------|
| 参与全链路联调（核心流程4涉及你的模块） | — |
| 修复联调中发现的Bug | — |
| **如果时间允许，支援进度落后的成员**（如成员3或成员4） | — |

---

## 你的产出物清单

| 类别 | 文件 | 数量 |
|------|------|------|
| 后端Entity | Blacklist, AuditLog | 2 |
| 后端Mapper | BlacklistMapper, AuditLogMapper | 2 |
| 后端Service | AdminService, AdminServiceImpl | 2 |
| 后端Controller | AdminController | 1 |
| 后端DTO | AuditDTO, DashboardVO | 2 |
| 前端工程 | admin-app项目（package.json, vite.config, App.tsx等） | 5 |
| 前端页面 | AdminLoginPage, AuditPage, DashboardPage | 3 |

**总计约 17 个核心文件**

---

## 你依赖谁？

```
成员1 ── 提供 @AdminOnly 注解（你的所有API需要这个注解保护）
成员1 ── 提供 JWT认证（管理员登录也需要JWT）
成员3 ── 提供商品数据（审核页面的商品来自product表）
成员2 ── 提供用户数据（统计页面的用户数来自user表）
成员4 ── 提供订单数据（统计页面的订单数来自order表）
```

---

## 谁依赖你？

```
（无）─ 你是最下游的模块，没有人依赖你的API
```

---

## 关键提醒

- **你的工作量最少**，所以请在Day5完成后主动帮助其他进度落后的成员
- **管理端是独立工程**（`admin-app/`），不与用户端（`user-app/`，成员5搭建）共用代码，但可以参考成员5的Axios封装方式
- **数据看板不做图表**，MVP阶段只展示统计数字，保持简洁
- **审核记录要存audit_log表**，方便追溯
- 管理员账号手动初始化：`INSERT INTO user (phone, password, role) VALUES ('13800000000', 'BCryptHash', 'ADMIN')`
- **加分项**：如果你的进度超前，可以实现黑名单管理和类目管理，这会让演示更加完整
